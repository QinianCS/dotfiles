# l -> locked, will also work when an input inhibitor (e.g. a lockscreen) is active.
# r -> release, will trigger on release of a key.
# o -> longPress, will trigger on long press of a key.
# e -> repeat, will repeat when held.
# n -> non-consuming, key/mouse events will be passed to the active window in addition to triggering the dispatcher.
# m -> mouse, see below.
# t -> transparent, cannot be shadowed by other binds.
# i -> ignore mods, will ignore modifiers.
# s -> separate, will arbitrarily combine keys between each mod/key, see [Keysym combos](#keysym-combos) above.
# d -> has description, will allow you to write a description for your bind.
# p -> bypasses the app's requests to inhibit keybinds.
bind = Super+Alt+Ctrl, Delete, exec, hyprctl dispatch exit
###g
# bindnr = CTRL, C, submap, ctrl_c_chain
# submap = ctrl_c_chain
#   bindnr =Ctrl , C, exec, goldendict "$(wl-paste)"
#   bindnr =CTRL , C, submap, reset
#   bind = , escape, submap, reset
#   bind = CTRL, escape, submap, reset
# submap = reset
# bindnr = CTRL, C, submap, reset
bindr =Ctrl , G, exec, goldendict "$(wl-paste)"

# bindsnr = Ctrl, Ctrl+C, exec, goldendict "$(wl-paste -p)"
# bindr = Ctrl, G, exec, goldendict "$(wl-paste -p)"
# Cursed stuff
## Make window not amogus large
# bind = Ctrl+Super, Backslash, resizeactive, exact 640 480 # [hidden]

###raise
bind = SUPER, 1, exec, raise --class "firefox" --launch "firefox"
# bind = SUPER, 2, exec, raise --class "org.kde.krusader" --launch "krusader"
bind = SUPER, 2, exec, raise --class "doublecmd" --launch "doublecmd"
bind = SUPER, 3, exec, raise --class "GoldenDict-ng" --launch "goldendict"
bind = SUPER, 4, exec, raise --class "codium" --launch "codium --enable-features=UseOzonePlatform,WaylandWindowDecorations --ozone-platform=wayland"
bind = SUPER, 8, exec, raise --class "audacious" --launch "audacious"
bind = SUPER, T, exec, raise --class "kitty" --launch "kitty"
bind = SUPER, E, exec, raise --class "org.kde.dolphin" --launch "dolphin"
bind = SUPER, F1, exec, raise --class "sumatrapdf-prerel-64.exe" --launch "wine /home/t/pw/pm/sumatrapdf-prerelease/SumatraPDF-prerel-64.exe"
bind = SUPER+Shift, X, exec, raise --class "fsearch" --launch "fsearch"
##hyprswitch
exec-once = hyprswitch init --show-title --custom-css=/home/t/dotfiles/hypr/switch/launcher.css &
$key = tab
$mod = alt
$reverse = grave

bind = $mod, $key, exec, hyprswitch gui --mod-key $mod --key $key --close mod-key-release --reverse-key=key=$reverse --sort-recent && hyprswitch dispatch
bind = $mod $reverse, $key, exec, hyprswitch gui --mod-key $mod --key $key --close mod-key-release --reverse-key=key=$reverse --sort-recent && hyprswitch dispatch -r

# use the if switching to the next window with the opening keypress is unwanted
#bind = $mod, $key, exec, hyprswitch gui --mod-key $mod --key $key --close mod-key-release --reverse-key=key=$reverse
#bind = alt $reverse, $key, exec, hyprswitch gui --mod-key $mod --key $key --close mod-key-release --reverse-key=key=$reverse
##! Media
bindl= Super+Shift, N, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"` # Next track
bindl= ,XF86AudioNext, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"` # [hidden]
bindl= ,XF86AudioPrev, exec, playerctl previous # [hidden]
# bindel = Super+Shift, Comma, exec, ~/.config/ags/scripts/music/adjust-volume.sh -0.03 # Raise music volume
# bindel = Super+Shift, Period, exec, ~/.config/ags/scripts/music/adjust-volume.sh 0.03 # Lower music volume
bind = Super+Shift+Alt, mouse:275, exec, playerctl previous # [hidden]
bind = Super+Shift+Alt, mouse:276, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"` # [hidden]
bindl= Super+Shift, B, exec, playerctl previous # Previous track
bindl= Super, F8, exec, playerctl play-pause # Play/pause media
bindl= ,XF86AudioPlay, exec, playerctl play-pause # [hidden]
bindl= ,XF86AudioPause, exec, playerctl play-pause # [hidden]
bindle=Super, F12, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 7%+ # [hidden]
bindle=Super, F11, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 7%- # [hidden]

#  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#  ┃                        Fullscreen                        ┃
#  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
bind = ALT, f, fullscreen#,2  # toggle fullscreen, without hiding the bar
bind = SUPER + CTRL, F, fullscreen # toggle fullscreen, hiding the bar

#  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#  ┃                      Toggle                              ┃
#  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
# bind = SUPER + CTRL, P, pseudo, # dwindle
# bind = SUPER + CTRL, S, togglesplit, # dwindle
# bind = SUPER + CTRL, Space, togglefloating,
# bind = SUPER + CTRL, G, togglegroup,
# bind = SUPER + CTRL, C, centerwindow

#  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#  ┃            Move active window to a workspace             ┃
#  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
bind = CTRL + ALT, 1, movetoworkspace, 1
bind = CTRL + ALT, 2, movetoworkspace, 2
bind = CTRL + ALT, 3, movetoworkspace, 3
bind = CTRL + ALT, 4, movetoworkspace, 4
bind = CTRL + ALT, 5, movetoworkspace, 5
bind = CTRL + ALT, 6, movetoworkspace, 6
bind = CTRL + ALT, 7, movetoworkspace, 7
bind = CTRL + ALT, 8, movetoworkspace, 8
bind = CTRL + ALT, 9, movetoworkspace, 9

#  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#  ┃                  some nice mouse binds                   ┃
#  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

#  ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#  ┃                      Resize windows                      ┃
#  ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
# binde = SUPER + SHIFT, right, resizeactive, 30 0
# binde = SUPER + SHIFT, left, resizeactive, -30 0
# binde = SUPER + SHIFT, up, resizeactive, 0 -30
# binde = SUPER + SHIFT, down, resizeactive, 0 30



##! Actions
# Screenshot, Record, OCR, Color picker, Clipboard history
bind = Super, V, exec, pkill fuzzel || cliphist list | fuzzel  --match-mode fzf --dmenu | cliphist decode | wl-copy # Clipboard history >> clipboard
# bind = Super, Period, exec, pkill fuzzel || ~/.local/bin/fuzzel-emoji # Pick emoji >> clipboard
bind = Super, Period, exec, pkill rofi || rofi -modi emoji -show emoji # Pick emoji >> clipboard
bind = Ctrl+Shift+Alt, Delete, exec, pkill wlogout || wlogout -p layer-shell # [hidden]
# bind = Super+Shift, S, exec, ~/.config/ags/scripts/grimblast.sh --freeze copy area # Screen snip
bind = Super+Shift+Alt, S, exec, grim -g "$(slurp)" - | swappy -f - # Screen snip >> edit
# OCR
bind = Super+Shift,T,exec,grim -g "$(slurp $SLURP_ARGS)" "tmp.png" && tesseract -l eng "tmp.png" - | wl-copy && rm "tmp.png" # Screen snip to text >> clipboard
bind = Ctrl+Super+Shift,S,exec,grim -g "$(slurp $SLURP_ARGS)" "tmp.png" && tesseract "tmp.png" - | wl-copy && rm "tmp.png" # [hidden]
# Color picker
bind = Super+Shift, C, exec, hyprpicker -a # Pick color (Hex) >> clipboard
# Fullscreen screenshot
bindl=,Print,exec,grim - | wl-copy # Screenshot >> clipboard
bindl= Ctrl,Print, exec, mkdir -p ~/Pictures/Screenshots && ~/.config/ags/scripts/grimblast.sh copysave screen ~/Pictures/Screenshots/Screenshot_"$(date '+%Y-%m-%d_%H.%M.%S')".png # Screenshot >> clipboard & file

bind = Super, L, exec, loginctl lock-session # Lock
bind = Super+Shift, L, exec, loginctl lock-session # [hidden]
bindl = Super+Shift, L, exec, sleep 0.1 && systemctl suspend || loginctl suspend # Suspend system
bind = Ctrl+Shift+Alt+Super, Delete, exec, systemctl poweroff || loginctl poweroff # [hidden] Power off


# bind = Super, BracketLeft, movefocus, l # [hidden]
# bind = Super, BracketRight, movefocus, r # [hidden]
# bindm = Super, mouse:272, movewindow
# bindm = Super, mouse:273, resizewindow
bind = Super, Q, exec, hyprctl dispatch killactive && hyprctl dispatch focuscurrentorlast
bind = Super+Shift+Alt, Q, exec, hyprctl kill # Pick and kill a window

# Window split ratio
#/# binde = Super, +/-,, # Window: split ratio +/- 0.1
binde = Super, Semicolon, splitratio, -0.1 # [hidden]
binde = Super, Apostrophe, splitratio, +0.1 # [hidden]
# Positioning mode
bind = Super+Alt, Space, togglefloating,
bind = Super+Alt, F, fullscreenstate, 0 3 # Toggle fake fullscreen
bind = Super, F, fullscreen, 0
bind = Super, D, fullscreenstate, 1

bind = Super, Tab, togglespecialworkspace,
# bind = Super, S, togglespecialworkspace,
# bind = Super, mouse:275, togglespecialworkspace,


#!
##! Widgets
# bindr = Ctrl+Super, R, exec, killall ags agsv1 ydotool; agsv1 & # Restart widgets
bindr = Ctrl+Super+Alt, R, exec, hyprctl reload; killall agsv1 ydotool; agsv1 & # [hidden]
# bind = Ctrl+Alt, Slash, exec, agsv1 run-js 'cycleMode();' # Cycle bar mode (normal, focus)
# bindir = Super, Super_L, exec, agsv1 -t 'overview' # Toggle overview/launcher
# bind = Super, Tab, exec, agsv1 -t 'overview' # [hidden]
# bind = Super, Slash, exec, for ((i=0; i<$(hyprctl monitors -j | jq length); i++)); do agsv1 -t "cheatsheet""$i"; done # Show cheatsheet
# bind = Super, B, exec, agsv1 -t 'sideleft' # Toggle left sidebar
# bind = Super, A, exec, agsv1 -t 'sideleft' # [hidden]
# bind = Super, O, exec, agsv1 -t 'sideleft' # [hidden]
# bind = Super, N, exec, agsv1 -t 'sideright' # Toggle right sidebar
# bind = Super, M, exec, agsv1 run-js 'openMusicControls.value = (!mpris.getPlayer() ? false : !openMusicControls.value);' # Toggle music controls
# bind = Super, Comma, exec, agsv1 run-js 'openColorScheme.value = true; Utils.timeout(2000, () => oppenColorScheme.value = false);' # View color scheme and options
# bind = Super, K, exec, for ((i=0; i<$(hyprctl monitors -j | jq length); i++)); do agsv1 -t "osk""$i"; done # Toggle on-screen keyboard
# bind = Ctrl+Alt, Delete, exec, for ((i=0; i<$(hyprctl monitors -j | jq length); i++)); do agsv1 -t "session""$i"; done # Toggle power menu
# bind = Ctrl+Super, G, exec, for ((i=0; i<$(hyprctl monitors -j | jq length); i++)); do agsv1 -t "crosshair""$i"; done # Toggle crosshair
# bindle=, XF86MonBrightnessUp, exec, agsv1 run-js 'brightness.screen_value += 0.05; indicator.popup(1);' # [hidden]
# bindle=, XF86MonBrightnessDown, exec, agsv1 run-js 'brightness.screen_value -= 0.05; indicator.popup(1);' # [hidden]
# bindl  = , XF86AudioMute, exec, agsv1 run-js 'indicator.popup(1);' # [hidden]
# bindl  = Super+Shift,M,   exec, agsv1 run-js 'indicator.popup(1);' # [hidden]

# Testing
# bind = SuperAlt, f12, exec, notify-send "Hyprland version: $(hyprctl version | head -2 | tail -1 | cut -f2 -d ' ')" "owo" -a 'Hyprland keybind'
# bind = Super+Alt, f12, exec, notify-send "Millis since epoch" "$(date +%s%N | cut -b1-13)" -a 'Hyprland keybind'
bind = Super+Alt, f12, exec, notify-send 'Test notification' "Here's a really long message to test truncation and wrapping\nYou can middle click or flick this notification to dismiss it!" -a 'Shell' -A "Test1=I got it!" -A "Test2=Another action" -t 5000 # [hidden]
bind = Super+Alt, Equal, exec, notify-send "Urgent notification" "Ah hell no" -u critical -a 'Hyprland keybind' # [hidden]


#!
##! Apps
# bind = Super, T, exec, # Launch foot (terminal)
# bind = Super, Z, exec, Zed # Launch Zed (editor)
# bind = Super, C, exec, code --password-store=gnome --enable-features=UseOzonePlatform --ozone-platform=wayland # Launch VSCode (editor)
# bind = Super, E, exec, nautilus --new-window # Launch Nautilus (file manager)
# bind = Super+Alt, E, exec, thunar # [hidden]
# bind = Super, W, exec, google-chrome-stable # [hidden] Let's not give people (more) reason to shit on my rice
# bind = Ctrl+Super, W, exec, firefox # Launch Firefox (browser)
# bind = Super, X, exec, gnome-text-editor --new-window # Launch GNOME Text Editor
# bind = Super+Shift, W, exec, wps # Launch WPS Office
# bind = Super, I, exec, XDG_CURRENT_DESKTOP="gnome" gnome-control-center # Launch GNOME Settings
bind = Ctrl+Super, V, exec, pavucontrol # Launch pavucontrol (volume mixer)
# bind = Ctrl+Super+Shift, V, exec, easyeffects # Launch EasyEffects (equalizer & other audio effects)
bind = Ctrl+Shift, Escape, exec, gnome-system-monitor # Launch GNOME System monitor
bind = Ctrl+Super, Slash, exec, pkill anyrun || anyrun # Toggle fallback launcher: anyrun
# bindir = , Super_L, exec, pkill anyrun || anyrun # Toggle fallback launcher: anyrun
# bindir = Super , Super_L, exec, pkill fuzzel || fuzzel # Toggle overview/launcher
# bindir = Super , Super_L, exec, pkill rofi || rofi -show drun 
bindir = Super , Super_L, exec, pkill rofi || rofi -show combi -modes combi -combi-modes "window,drun,run"
# bindir = Super , Super_L, exec, kill -s SIGUSR1 rofi || rofi -no-lazy-grab -show combi -modes combi -combi-modes "window,drun,run" -lazy-grab &
# bind = Super , Tab, exec, pkill rofi || rofi -show window # Toggle overview/launcher
# bind = Super , Tab, exec, pkill rofi || rofi -show combi -modes combi -combi-modes "window,drun,run"
# bind = Alt , Space, exec, pkill rofi || rofi -show run # Toggle overview/launcher
bind = Alt , Space, exec, pkill fuzzel || fuzzel
bind = Super+Shift , W, exec, pkill waybar || waybar
# bindir = Super, Super_L, exec, agsv1 -t 'overview' # Toggle overview/launcher


















## scroller##
$mainMod = Super
###scroller misc
bind = $mainMod, Page_Down,  movetoworkspacesilent, special # [hidden]
bind = $mainMod, Page_Up,  movetoworkspacesilent, 1 # [hidden]
# You can put your preferred keybinds here
# https://wiki.hyprland.org/Configuring/Binds/
# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod, home, scroller:movefocus, begin
bind = $mainMod, end, scroller:movefocus, end

# Movement
bind = $mainMod CTRL, left, movewindow, l
bind = $mainMod CTRL, right, movewindow, r
bind = $mainMod CTRL, up, movewindow, u
bind = $mainMod CTRL, down, movewindow, d
bind = $mainMod CTRL, home, scroller:movewindow, begin
bind = $mainMod CTRL, end, scroller:movewindow, end
bind = $mainMod ALT, left, movewindow, l, nomode
bind = $mainMod ALT, right, movewindow, r, nomode
bind = $mainMod ALT, up, movewindow, u, nomode
bind = $mainMod ALT, down, movewindow, d, nomode
bind = $mainMod ALT, home, scroller:movewindow, begin, nomode
bind = $mainMod ALT, end, scroller:movewindow, end, nomode

# Modes
bind = $mainMod, bracketleft, scroller:setmode, row
bind = $mainMod, bracketright, scroller:setmode, col

# # Set Mode Modifiers submap
# # will switch to a submap called modifiers
# bind = $mainMod, backslash, submap, modifiers
# # will start a submap called "modifiers"
# submap = modifiers
# bind = , right, scroller:setmodemodifier, after
# bind = , right, submap, reset
# bind = , left, scroller:setmodemodifier, before
# bind = , left, submap, reset
# bind = , home, scroller:setmodemodifier, beginning
# bind = , home, submap, reset
# bind = , end, scroller:setmodemodifier, end
# bind = , end, submap, reset
# bind = , up, scroller:setmodemodifier, focus
# bind = , up, submap, reset
# bind = , down, scroller:setmodemodifier, nofocus
# bind = , down, submap, reset
# bind = , 2, scroller:setmodemodifier, auto:2
# bind = , 2, submap, reset
# bind = , 3, scroller:setmodemodifier, auto:3
# bind = , 3, submap, reset
# bind = , m, scroller:setmodemodifier, manual
# bind = , m, submap, reset
# bind = , c, scroller:setmodemodifier, center_column
# bind = , c, submap, reset
# bind = SHIFT, c, scroller:setmodemodifier, nocenter_column
# bind = SHIFT, c, submap, reset
# bind = , w, scroller:setmodemodifier, center_window
# bind = , w, submap, reset
# bind = SHIFT, w, scroller:setmodemodifier, nocenter_window
# bind = SHIFT, w, submap, reset
# # use reset to go back to the global submap
# bind = , escape, submap, reset
# # will reset the submap, meaning end the current one and return to the global one
# submap = reset

# Sizing keys
# bind = $mainMod, equal, scroller:cyclesize, next
# bind = $mainMod, minus, scroller:cyclesize, prev
bind = $mainMod, equal, scroller:cyclewidth, next
bind = $mainMod, minus, scroller:cyclewidth, prev
bind = $mainMod SHIFT, equal, scroller:cycleheight, next
bind = $mainMod SHIFT, minus, scroller:cycleheight, prev

# # Sizes submap
# # will switch to a submap called sizing
# bind = $mainMod, b, submap, sizing
# # will start a submap called "align"
# submap = sizing
# # sets repeatable binds for aligning the active window
# bind = , 1, scroller:setsize, oneeighth
# bind = , 1, submap, reset
# bind = , 2, scroller:setsize, onesixth
# bind = , 2, submap, reset
# bind = , 3, scroller:setsize, onefourth
# bind = , 3, submap, reset
# bind = , 4, scroller:setsize, onethird
# bind = , 4, submap, reset
# bind = , 5, scroller:setsize, threeeighths
# bind = , 5, submap, reset
# bind = , 6, scroller:setsize, onehalf
# bind = , 6, submap, reset
# bind = , 7, scroller:setsize, fiveeighths
# bind = , 7, submap, reset
# bind = , 8, scroller:setsize, twothirds
# bind = , 8, submap, reset
# bind = , 9, scroller:setsize, threequarters
# bind = , 9, submap, reset
# bind = , 0, scroller:setsize, fivesixths
# bind = , 0, submap, reset
# bind = , minus, scroller:setsize, seveneighths
# bind = , minus, submap, reset
# bind = , equal, scroller:setsize, one
# bind = , equal, submap, reset
# # use reset to go back to the global submap
# bind = , escape, submap, reset
# # will reset the submap, meaning end the current one and return to the global one
# submap = reset

# Admit/Expel
bind = $mainMod, I, scroller:admitwindow,
bind = $mainMod, O, scroller:expelwindow,
bind = $mainMod SHIFT, I, scroller:admitwindow, r
bind = $mainMod SHIFT, O, scroller:expelwindow, l

# # Center submap
# # will switch to a submap called center
# bind = $mainMod, C, submap, center
# # will start a submap called "center"
# submap = center
# # sets repeatable binds for resizing the active window
# bind = , C, scroller:alignwindow, c
# bind = , C, submap, reset
# bind = , m, scroller:alignwindow, m
# bind = , m, submap, reset
# bind = , right, scroller:alignwindow, r
# bind = , right, submap, reset
# bind = , left, scroller:alignwindow, l
# bind = , left, submap, reset
# bind = , up, scroller:alignwindow, u
# bind = , up, submap, reset
# bind = , down, scroller:alignwindow, d
# bind = , down, submap, reset
# # use reset to go back to the global submap
# bind = , escape, submap, reset
# # will reset the submap, meaning end the current one and return to the global one
# submap = reset

# # Resize submap
# # will switch to a submap called resize
# bind = $mainMod SHIFT, R, submap, resize
# # will start a submap called "resize"
# submap = resize
# # sets repeatable binds for resizing the active window
# binde = , right, resizeactive, 100 0
# binde = , left, resizeactive, -100 0
# binde = , up, resizeactive, 0 -100
# binde = , down, resizeactive, 0 100
# # use reset to go back to the global submap
# bind = , escape, submap, reset
# # will reset the submap, meaning end the current one and return to the global one
# submap = reset

# # Fit size submap
# # will switch to a submap called fitsize
# bind = $mainMod, W, submap, fitsize
# # will start a submap called "fitsize"
# submap = fitsize
# # sets binds for fitting columns/windows in the screen
# bind = , W, scroller:fitsize, visible
# bind = , W, submap, reset
# bind = , right, scroller:fitsize, toend
# bind = , right, submap, reset
# bind = , left, scroller:fitsize, tobeg
# bind = , left, submap, reset
# bind = , up, scroller:fitsize, active
# bind = , up, submap, reset
# bind = , down, scroller:fitsize, all
# bind = , down, submap, reset
# bind = , bracketleft, scroller:fitwidth, all
# bind = , bracketleft, submap, reset
# bind = , bracketright, scroller:fitheight, all
# bind = , bracketright, submap, reset
# # use reset to go back to the global submap
# bind = , escape, submap, reset
# # will reset the submap, meaning end the current one and return to the global one
# submap = reset

# # overview keys
# # bind key to toggle overview (normal)
# bind = $mainMod, tab, scroller:toggleoverview
# bind = ,mouse:275, scroller:toggleoverview

# # Marks
# bind = $mainMod, M, submap, marksadd
# submap = marksadd
# bind = , a, scroller:marksadd, a
# bind = , a, submap, reset
# bind = , b, scroller:marksadd, b
# bind = , b, submap, reset
# bind = , c, scroller:marksadd, c
# bind = , c, submap, reset
# bind = , escape, submap, reset
# submap = reset

# bind = $mainMod SHIFT, M, submap, marksdelete
# submap = marksdelete
# bind = , a, scroller:marksdelete, a
# bind = , a, submap, reset
# bind = , b, scroller:marksdelete, b
# bind = , b, submap, reset
# bind = , c, scroller:marksdelete, c
# bind = , c, submap, reset
# bind = , escape, submap, reset
# submap = reset

# bind = $mainMod, apostrophe, submap, marksvisit
# submap = marksvisit
# bind = , a, scroller:marksvisit, a
# bind = , a, submap, reset
# bind = , b, scroller:marksvisit, b
# bind = , b, submap, reset
# bind = , c, scroller:marksvisit, c
# bind = , c, submap, reset
# bind = , escape, submap, reset
# submap = reset

# bind = $mainMod CTRL, M, scroller:marksreset

# Pin
bind = $mainMod, P, scroller:pin,

# Window copy/paste
bind = $mainMod, Insert, scroller:selectiontoggle,
bind = $mainMod CTRL, Insert, scroller:selectionreset,
bind = $mainMod SHIFT, Insert, scroller:selectionmove, right
bind = $mainMod CTRL SHIFT, Insert, scroller:selectionworkspace,

# # Trails and Trailmarks
# bind = $mainMod SHIFT, semicolon, submap, trail
# submap = trail
# bind = , bracketright, scroller:trailnext,
# bind = , bracketleft, scroller:trailprevious,
# bind = , semicolon, scroller:trailnew,
# bind = , semicolon, submap, reset
# bind = , d, scroller:traildelete,
# bind = , d, submap, reset
# bind = , c, scroller:trailclear,
# bind = , c, submap, reset
# bind = , Insert, scroller:trailtoselection,
# bind = , Insert, submap, reset
# bind = , escape, submap, reset
# submap = reset

# bind = $mainMod, semicolon, submap, trailmark
# submap = trailmark
# bind = , bracketright, scroller:trailmarknext,
# bind = , bracketleft, scroller:trailmarkprevious,
# bind = , semicolon, scroller:trailmarktoggle,
# bind = , semicolon, submap, reset
# bind = , escape, submap, reset
# submap = reset

bind = $mainMod, slash, scroller:jump,
bind = Alt, grave, scroller:jump,